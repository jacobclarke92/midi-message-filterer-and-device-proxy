{
  "name": "ohmrgb-fixer",
  "version": "1.0.0",
  "description": "MIDI Proxy to filter spammy CC messages",
  "main": "backend/server.js",
  "scripts": {
    "dev:backend": "node backend/server.js",
    "dev:frontend": "cd frontend && pnpm dev",
    "dev": "concurrently \"pnpm run dev:backend\" \"pnpm run dev:frontend\""
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "cors": "^2.8.6",
    "easymidi": "^3.2.0",
    "fastify": "^5.10.0",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
